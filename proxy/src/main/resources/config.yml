# SectorSystem Proxy Configuration
limbo:
  name: "limbo"
  display-name: "Poczekalnia"

redis:
  host: localhost
  port: 6379
  max-connections: 10

nats:
  url: nats://localhost:4222

mysql:
  enabled: true
  host: localhost
  port: 3306
  database: sectorsystem
  username: root
  password: ""

auth:
  login-timeout: 60
  auto-register: true
  default-server: guild
